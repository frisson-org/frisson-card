const TitleDistance = ( { distance } ) => {

	return (
		<div className="f-title__distance flex aic">
			<span className="f-title__distance-value block">{ distance }</span>
		</div>
	);
}

export default TitleDistance;
